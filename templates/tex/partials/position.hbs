\begin{tabular*}{6.6in}{@{\extracolsep{\fill}}lr}
\textbf{ {{company}} } & \textbf{ {{location}} }\\
\textit{ {{department}} } & \textit{ {{start}} -- {{end}} }
\end{tabular*}
{{#each positions as |position|}}
  {{position.title}}
  \begin{compactitem}
  {{#each position.tasks}}
    \item {{{this}}}
  {{/each}} \medskip
  \end{compactitem}
{{/each}}
